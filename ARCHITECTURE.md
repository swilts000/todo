# System Architecture & Data Flow

## 🏗️ Application Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        BROWSER / CLIENT                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           Frontend (HTML/CSS/JavaScript)                │  │
│  │  ┌────────────────────────────────────────────────────┐ │  │
│  │  │ app.js - Enhanced with:                            │ │  │
│  │  │  • Error parsing from backend                      │ │  │
│  │  │  • Success/error notifications                     │ │  │
│  │  │  • Console logging                                 │ │  │
│  │  │  • Detailed error messages                         │ │  │
│  │  └────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                            ↕
                    HTTP REST API
              /api/todos endpoints
                            ↕
┌─────────────────────────────────────────────────────────────────┐
│                  BACKEND / SPRING BOOT                           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │        TodoController.java - Enhanced with:             │  │
│  │  • Comprehensive error handling                         │  │
│  │  • SLF4J logging on every operation                     │  │
│  │  • Proper HTTP status codes (201/500)                  │  │
│  │  • Error response objects                               │  │
│  │  ┌────────────────────────────────────────────────────┐ │  │
│  │  │ GET /api/todos      → List all todos               │ │  │
│  │  │ POST /api/todos     → Create todo (201 or error)   │ │  │
│  │  │ PUT /api/todos/{id} → Update todo                  │ │  │
│  │  │ DELETE /api/todos/{id} → Delete todo               │ │  │
│  │  └────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │        TodoRepository (JPA)                             │  │
│  │  • Interface extends JpaRepository                      │  │
│  │  • Handles database persistence                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │        Application Configuration                        │  │
│  │  • application.properties (Docker)                      │  │
│  │  • application-local.properties (Development)           │  │
│  │  • HikariCP connection pooling                          │  │
│  │  • SLF4J logging configuration                          │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │        ApplicationStartupListener                       │  │
│  │  • Logs database URL on startup                         │  │
│  │  • Confirms connection details                          │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                            ↕
                    JDBC + HikariCP
              Connection pooling & timeouts
                            ↕
┌─────────────────────────────────────────────────────────────────┐
│                    MYSQL DATABASE                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Database: tododb                                       │  │
│  │  ┌──────────────────────────────────────────────────┐  │  │
│  │  │  Table: todo                                     │  │  │
│  │  │  ┌──────────────┬──────────┬──────────────────┐ │  │  │
│  │  │  │ id           │ text     │ done             │ │  │  │
│  │  │  │ (BIGINT PK)  │ (VARCHAR)│ (BOOLEAN)        │ │  │  │
│  │  │  ├──────────────┼──────────┼──────────────────┤ │  │  │
│  │  │  │ 1            │ Buy milk │ false            │ │  │  │
│  │  │  │ 2            │ Get eggs │ false            │ │  │  │
│  │  │  └──────────────┴──────────┴──────────────────┘ │  │  │
│  │  └──────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Request/Response Flow

### Successful Todo Creation

```
┌─────────────────────────────────────────────────────────────────┐
│ USER CLICKS "ADD" BUTTON                                         │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ FRONTEND (app.js)                                                │
│ • Gets input value: "Buy groceries"                             │
│ • Validates: not empty ✓                                         │
│ • Sends POST to /api/todos                                       │
│   {                                                              │
│     "text": "Buy groceries",                                     │
│     "done": false                                                │
│   }                                                              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                    HTTP POST Request
                   Content-Type: JSON
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ BACKEND (TodoController)                                         │
│ • Receives request                                               │
│ • Validates input: text is not empty ✓                          │
│ • Logs: "Creating new todo with text: Buy groceries"            │
│ • Saves to repository                                            │
│ • Logs: "Successfully saved todo with ID: 1"                    │
│ • Returns HTTP 201 Created                                       │
│   {                                                              │
│     "id": 1,                                                     │
│     "text": "Buy groceries",                                     │
│     "done": false                                                │
│   }                                                              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                 HTTP 201 Response + JSON
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ DATABASE (MySQL)                                                 │
│ • INSERT INTO todo (text, done) VALUES (?, ?)                   │
│ • Data saved with ID: 1                                          │
│ • Committed to database                                          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
        Response received with saved todo object
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ FRONTEND (app.js)                                                │
│ • Parse response: HTTP 201 ✓                                    │
│ • Extract ID: 1                                                  │
│ • Show notification: "✓ Todo added successfully! (ID: 1)" 🟢    │
│ • Clear input field                                              │
│ • Reload todos list                                              │
│ • Display: "Buy groceries" in the list                          │
│ • Log to console: "[SUCCESS] Todo added successfully!"          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ USER SEES                                                        │
│ ✓ Green notification: "✓ Todo added successfully! (ID: 1)"     │
│ ✓ Todo appears in list: "Buy groceries"                        │
│ ✓ User KNOWS data was saved to database                        │
└─────────────────────────────────────────────────────────────────┘
```

---

### Error Case - MySQL Connection Failed

```
┌─────────────────────────────────────────────────────────────────┐
│ USER CLICKS "ADD" BUTTON (MySQL is Down)                        │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ FRONTEND (app.js)                                                │
│ • Gets input value: "Buy groceries"                             │
│ • Validates: not empty ✓                                         │
│ • Sends POST to /api/todos                                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ BACKEND (TodoController)                                         │
│ • Receives request                                               │
│ • Tries to save to repository                                    │
│ • MySQL Connection Error! ❌                                     │
│ • CATCH block executes                                           │
│ • Logs: "[ERROR] Error creating todo: Connection refused        │
│          java.sql.SQLNonTransientConnectionException:            │
│          Cannot get a connection..."                             │
│ • Returns HTTP 500 Internal Server Error                         │
│   {                                                              │
│     "error": "Failed to create todo: Connection refused to       │
│               MySQL server on 'mysql:3306'"                      │
│   }                                                              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
           HTTP 500 Response + Error JSON
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ FRONTEND (app.js)                                                │
│ • Check response: HTTP 500 ❌                                    │
│ • Call parseErrorResponse()                                      │
│ • Extract error: "Connection refused to MySQL server..."         │
│ • Show notification: "Error adding todo: Connection refused      │
│                      to MySQL server..." 🔴                      │
│ • Log to console: "[ERROR] Error adding todo: Connection..."    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ USER SEES                                                        │
│ ❌ Red notification: "Error adding todo: Connection refused..."│
│ ❌ Todo does NOT appear in list                                │
│ ✓ User KNOWS exactly what went wrong (MySQL is down)           │
│ ✓ Can report specific issue to developer/ops                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

```
                    SUBMISSION FLOW
                    ═══════════════

BROWSER                          SERVER                        DATABASE
  │                                │                               │
  │  User types "Buy groceries"    │                               │
  │  and clicks "Add"              │                               │
  │                                │                               │
  ├─────────POST JSON──────────────>                               │
  │  {text: "Buy groceries"}        │                               │
  │                                │  [Controller receives]        │
  │                                ├─ Validate input ✓            │
  │                                ├─ Log start                    │
  │                                ├────INSERT INTO todo────────>  │
  │                                │                               │
  │                                │        [MySQL executes]       │
  │                                │  Returns: id=1 (auto)         │
  │                                │  <─────ID=1──────────────    │
  │                                ├─ Log success                  │
  │  <────HTTP 201 + JSON──────────┤                               │
  │  {id: 1, text: "...", done:0}   │                               │
  │                                │                               │
  ├─ Parse response                │                               │
  ├─ Extract ID: 1                 │                               │
  ├─ Show notification             │                               │
  ├─ Add to list                   │                               │
  │                                │                               │
  └─ GET /api/todos ──────────────>│                               │
     (to refresh list)              │  [Fetch all]                 │
                                    ├────SELECT * FROM todo───────>│
                                    │                               │
                                    │        [Returns all todos]    │
                                    │  <──[id=1, text="Buy..."]    │
                                    │  <──[id=2, text="Get..."]    │
                                    │                               │
     <────HTTP 200 + Array─────────┤                               │
     [{id:1,...}, {id:2,...}]       │                               │
     │                               │
     └─ Render list                 │
       "• Buy groceries"             │
       "• Get eggs"                  │
```

---

## 📈 Error Handling Flow

```
ERROR DETECTION
  ↓
┌─────────────────────────────────────────┐
│ Backend Exception Caught                │
│ try-catch block in Controller           │
└─────────────────────────────────────────┘
  ↓
┌─────────────────────────────────────────┐
│ Logger Error                            │
│ logger.error("Error creating todo", e)  │
│ Writes to console + log file            │
└─────────────────────────────────────────┘
  ↓
┌─────────────────────────────────────────┐
│ Error Message Extracted                 │
│ e.getMessage() gets specific cause      │
└─────────────────────────────────────────┘
  ↓
┌─────────────────────────────────────────┐
│ JSON Error Response Created             │
│ {                                       │
│   "error": "Failed to create todo: ..   │
│ }                                       │
└─────────────────────────────────────────┘
  ↓
┌─────────────────────────────────────────┐
│ HTTP 500 Status Code                    │
│ ResponseEntity.status(500).body(error)  │
└─────────────────────────────────────────┘
  ↓
┌─────────────────────────────────────────┐
│ Frontend Receives Response              │
│ if (!res.ok) {                          │
│   const err = await parseErrorResponse()│
│   showNotification(err, "error")        │
│ }                                       │
└─────────────────────────────────────────┘
  ↓
┌─────────────────────────────────────────┐
│ User Sees Red Notification              │
│ "Error adding todo: [specific error]"   │
└─────────────────────────────────────────┘
```

---

## 🔐 Security & Data Flow

```
BROWSER              NETWORK              SERVER              DATABASE
  │                    │                   │                    │
  │  HTTPS/HTTP        │                   │                    │
  ├──────validation─────>                  │                    │
  │  (non-empty text)    │                 │                    │
  │                     │                  │                    │
  │                     │  JSON Body       │                    │
  │                     │  {text: "..."}   │                    │
  │                     ├─────────────────>│                    │
  │                     │                  │                    │
  │                     │                  │  Input Validation  │
  │                     │                  ├─ Check not null    │
  │                     │                  ├─ Check not empty   │
  │                     │                  │                    │
  │                     │                  │  Parameterized     │
  │                     │                  │  SQL Query         │
  │                     │                  │  (prevents SQL    │
  │                     │                  │   injection)       │
  │                     │                  ├──────────────────>│
  │                     │                  │  INSERT INTO todo  │
  │                     │                  │  (text, done)      │
  │                     │                  │  VALUES (?, ?)     │
  │                     │                  │                    │
  │                     │                  │  Response: id+1    │
  │                     │                  │  <──────────────────
  │                     │                  │                    │
  │                     │  JSON Response   │                    │
  │                     │  {id:1,text:"..."│                    │
  │                     │<──────────────────│                    │
  │  Response           │                   │                    │
  │<────────────────────┤                   │                    │
  │                     │                   │                    │
```

**Security Features:**
- ✅ Parameterized SQL queries (prevents injection)
- ✅ Input validation on backend
- ✅ Try-catch exception handling
- ✅ No sensitive data in logs
- ✅ Proper HTTP status codes

---

## 🚀 Deployment Architecture

```
DOCKER ENVIRONMENT
════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────┐
│                    docker-compose network                    │
│  ┌──────────────────┐  ┌──────────────────┐  ┌────────────┐ │
│  │ FRONTEND         │  │ BACKEND          │  │ MYSQL      │ │
│  │ (nginx:latest)   │  │ (Java:17)        │  │ (8.0)      │ │
│  │                  │  │                  │  │            │ │
│  │ port: 8081       │  │ port: 8080       │  │ port: 3306 │ │
│  │                  │  │                  │  │            │ │
│  │ - index.html     │  │ - TodoApp        │  │ - tododb   │ │
│  │ - app.js         │  │ - application    │  │ - todo     │ │
│  │ - styles.css     │  │   .properties    │  │   table    │ │
│  └────────┬─────────┘  └────────┬─────────┘  └────────┬───┘ │
│           │ (HTTP/JSON)         │ (JDBC)              │      │
│           │<──────────────────────────────────────────>      │
│           │                     │                     │      │
│           └─────────────────────┴─────────────────────┘      │
│                     (Docker network)                         │
└──────────────────────────────────────────────────────────────┘
                           ↓
                        localhost
         http://localhost:8081 (browser)
         http://localhost:8080/api/todos (API)
         localhost:3306 (MySQL)
```

**Docker Compose Structure:**
```yaml
services:
  mysql:
    - Runs on mysql:3306 (inside Docker network)
    - Health check enabled
    - Persistent volume: mysql_data
    
  backend:
    - Runs on localhost:8080
    - Connects to mysql:3306
    - Environment: DB_PASSWORD
    - Depends on: mysql (healthcheck)
    
  frontend:
    - Runs on localhost:8081
    - Serves static files (index.html, etc.)
    - Proxies /api/* to backend:8080
    - Depends on: backend

volumes:
  mysql_data: (persists database between restarts)
```

---

## 🔄 Logging & Monitoring

```
                        APPLICATION FLOW
                        ═════════════════

STARTUP PHASE:
┌─────────────────────────────────────────────────────┐
│ [INFO] Started TodoApplication                      │
│ [INFO] Todo Application Started Successfully!       │
│ [INFO] Database URL: jdbc:mysql://mysql:3306/tododb│
│ [INFO] Database Username: root                      │
│ [INFO] Server Port: 8080                            │
└─────────────────────────────────────────────────────┘
              ↓
OPERATIONAL PHASE (Operations are logged):
┌─────────────────────────────────────────────────────┐
│ [INFO] Fetching all todos from database             │
│ [DEBUG] Executing SELECT * FROM todo                │
│ [INFO] Successfully fetched 2 todos                 │
│                                                     │
│ [INFO] Creating new todo with text: Buy groceries  │
│ [DEBUG] Executing INSERT INTO todo (text, done)    │
│ [DEBUG] Binding parameter [1] as [VARCHAR]          │
│ [DEBUG] Binding parameter [2] as [BOOLEAN]          │
│ [INFO] Successfully saved todo with ID: 1          │
│                                                     │
│ [INFO] Deleting todo with ID: 1                    │
│ [DEBUG] Executing DELETE FROM todo WHERE id=1      │
│ [INFO] Successfully deleted todo with ID: 1        │
└─────────────────────────────────────────────────────┘
              ↓
ERROR PHASE (Errors are caught & logged):
┌─────────────────────────────────────────────────────┐
│ [INFO] Creating new todo with text: Test...         │
│ [DEBUG] Executing INSERT INTO todo...               │
│ [ERROR] Error creating todo                         │
│ java.sql.SQLNonTransientConnectionException:        │
│   Cannot get a connection, pool error Timeout       │
│   waiting for an available connection               │
│ [WARN] Failed to create todo: Connection refused... │
└─────────────────────────────────────────────────────┘
```

**Log Levels:**
- **TRACE**: SQL parameter bindings (very verbose)
- **DEBUG**: SQL execution details
- **INFO**: Operation summaries (normal flow)
- **ERROR**: Exceptions and failures

---

## ✨ Summary

The architecture now provides:

1. **Clear User Feedback** - Success/error notifications
2. **Detailed Logging** - Every operation tracked
3. **Error Handling** - Graceful failure recovery
4. **Data Persistence** - Confirmed by ID in notification
5. **Developer Friendly** - Logs tell the full story
6. **Production Ready** - Proper error codes and messages

**The entire system is designed to make it obvious whether data was successfully saved!**

